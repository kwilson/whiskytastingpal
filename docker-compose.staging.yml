version: '3'
services:
  web:
    image: kyma/docker-nginx
    depends_on:
      - builder
      - api
    volumes:
      - ./web/nginx-config:/etc/nginx/sites-enabled/default:ro
      - ./web/dist:/var/www
    ports:
      - "8080:80"
